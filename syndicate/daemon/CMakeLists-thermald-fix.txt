# Leatherhead – thermal monitoring
add_executable(thermald
    thermald.c
    fugitoid_log.c
    krang.c
)

target_include_directories(thermald
    PRIVATE
        ${CMAKE_SOURCE_DIR}/src/core/include
        ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(thermald
    PRIVATE
        sensei_core
        crypto
)

install(TARGETS thermald
    RUNTIME DESTINATION bin
)
