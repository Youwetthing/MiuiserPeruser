Database layer (MiuiserPeruser)

- Stores structured runtime state
- Backend results (ADB / Shizuku / Rish / sysport)
- Capability snapshots
- Foot Clan job outputs (if persisted)

This is NOT a log system. It is authoritative state storage.
